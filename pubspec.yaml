name: alertapp
description: "A new Flutter project for mass alerts."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # حزم فايربيس
  firebase_core: ^2.24.2
  firebase_messaging: ^14.7.10
  # حزمة شاشة الاتصال الوارد
  flutter_callkit_incoming: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
